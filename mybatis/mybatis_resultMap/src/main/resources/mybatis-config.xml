<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--  核心配置文件中所需要引入的顺序   "(properties?,settings?,typeAliases?,typeHandlers?,objectFactory?,objectWrapperFactory?
                ,reflectorFactory?,plugins?,environments?,databaseIdProvider?,mappers?)".-->
    <!--    mybatis-config这个文件可以不是固定的 -->
    <!--引入外部配置文件
          resource：表示的是相对于resource目录地址
     -->

    <properties resource="dp.properties"/>
    <!--    配置数据库的连接-->
    <!--    environments: 配置数据库的环境
            id: 设置环境的唯一标识，不能重复
    -->
    <!--    typeAliases : 设置类型别名
            对接resultType和resultMap
    -->

    <settings>
<!--        将数据库下划线设置为驼峰-->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <typeAliases>
<!--        <typeAlias type="com.atguigu.mybatis.pojo.User" alias="user"></typeAlias>-->
        <!--        通过设置包的别名，指定包下所有类型将全部拥有默认的别名，即类名且不区分大小写-->
        <package name="com.atguigu.mybatis.pojo" />
    </typeAliases>

    <environments default="development">
        <environment id="development">

            <!--            transactionManager: 设置事务的管理器
                            属性：
                            type: 设置事务管理方式
                            type=JDBC :表示使用JDBC原生的事务管理方式
                            type=MANAGED: 被管理，例如Spring

            -->
            <transactionManager type="JDBC"/>
            <!--            datasource: 设置数据源属性：
                            type: 设置数据源类型
            -->
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 引入映射配置文件  -->
    <mappers>
<!--        <mapper resource="mappers/UserMapper.xml"/>-->
        <!--       以包的方式来引入映射文件，但是必须满足两个条件
                    1、mapper接口和映射文件所在的包必须一致
                    2、mapper接口的名字和映射文件的名字必须一致
         -->
                <package name="com.atguigu.mybatis.mapper"/>
    </mappers>
</configuration>